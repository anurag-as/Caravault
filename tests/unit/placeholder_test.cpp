#include <gtest/gtest.h>

// Placeholder test to allow unit_tests executable to build
// Real unit tests will be added in subsequent tasks

TEST(PlaceholderTest, AlwaysPasses) {
    EXPECT_TRUE(true);
}
